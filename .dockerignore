# ═════════════════════════════════════════════════════════════════════
# Docker build context 排除规则（packages/server/Dockerfile 用）
#
# 注意：仓库根作为 build context 时，前端打包产物 / Electron 二进制 / 测试缓存
# 都不应该进入镜像。
# ═════════════════════════════════════════════════════════════════════

# 依赖目录（builder 阶段会重新 npm ci）
node_modules
**/node_modules

# 构建产物
**/dist
**/dist-electron
**/release
**/coverage

# 客户端不参与后端 docker 镜像
packages/client

# 服务端运行时数据 / 上传文件 — 必须用 volume 挂载，不能进镜像
packages/server/data
packages/server/uploads
packages/server/logs

# 测试文件（运行时不需要）
**/__tests__
**/*.test.ts
**/*.spec.ts

# 环境变量与密钥
.env
.env.*
**/.env
**/.env.*

# IDE / 工具缓存
.git
.github
.vscode
.idea
.claude
.gstack
.cache

# 文档与设计稿
docs
*.pen
*.md
!packages/server/README.md

# 日志
*.log
**/*.log

# 临时文件
.DS_Store
Thumbs.db
*.tsbuildinfo
